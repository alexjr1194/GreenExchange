READ ME
directions:

npm install --save sequelize@2.0.0-rc1 sequelize-cli sqlite3

// running the following will create folders, config migrations, and models
node_modules/.bin/sequelize init

npm install express-ping --save